<!-- ⚠️  AUTO-GENERATED by scripts/capture_cli_evidence.py — DO NOT EDIT -->

# CLI Evidence: Config

### config get region（默认值）

```
$ sbox config get region
cn-hangzhou
Exit code: 0
```

### config get api_key（未设置）

```
$ sbox config get api_key
(not set)
Exit code: 0
```

### config get api_key（已设置, masked）

```
$ sbox config get api_key
sk-***456
Exit code: 0
```

### config get unknown_key（错误）

```
$ sbox config get unknown_key
Exit code: 2
```

**stderr:**

```
Unknown config key: 'unknown_key'
  Suggestion: Available keys: api_key, api_url, domain, http_timeout, llm_api_key, llm_base_url, llm_model, max_retries, region
```

### config set region

```
$ sbox config set region cn-shanghai
Set region = cn-shanghai
Exit code: 0
```

### config set llm_api_key（masked）

```
$ sbox config set llm_api_key sk-mykey12345678
Set llm_api_key = sk-***678
Exit code: 0
```

### config list

```
$ sbox config list
api_key       (not set)
api_url       https://api.cn-hangzhou.e2b.fc.aliyuncs.com (default)
domain        cn-hangzhou.e2b.fc.aliyuncs.com (default)
http_timeout  30.0 (default)
llm_api_key    (default)
llm_base_url   (default)
llm_model      (default)
max_retries   3 (default)
region        cn-hangzhou (default)
Exit code: 0
```

### config list --json

```
$ sbox --json config list
{
  "api_key": "(not set)",
  "api_url": "https://api.cn-hangzhou.e2b.fc.aliyuncs.com (default)",
  "domain": "cn-hangzhou.e2b.fc.aliyuncs.com (default)",
  "http_timeout": "30.0 (default)",
  "llm_api_key": " (default)",
  "llm_base_url": " (default)",
  "llm_model": " (default)",
  "max_retries": "3 (default)",
  "region": "cn-hangzhou (default)"
}
Exit code: 0
```

### config reset --yes

```
$ sbox config reset --yes
Configuration reset to defaults.
Exit code: 0
```
